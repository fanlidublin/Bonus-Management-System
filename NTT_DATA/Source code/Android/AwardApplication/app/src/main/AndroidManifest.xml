<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="award.topic.com.awardapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/c"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".RoleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ProjectAwardActivity" />
        <activity android:name=".PayAwardActivity" />
        <activity android:name=".AwardActivity" />
        <activity android:name=".AwardNoActivity" />
        <activity android:name=".ProjectNewsActivity" />
        <activity android:name=".AnalysisActivity"></activity>
    </application>

</manifest>